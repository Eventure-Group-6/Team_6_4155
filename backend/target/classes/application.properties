spring.application.name=backend

# H2 Database Configuration (Persistent)
spring.datasource.url=jdbc:h2:file:./data/eventure;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Flyer directory for H2 setup
app.flyer.base-dir=/tmp/flyerFolder

# Flyway configuration - commented out for H2 setup
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration

# MySQL configuration - commented out for H2 setup
# spring.datasource.url=jdbc:mysql://localhost:3306/eventure
# spring.datasource.username=eventure_user
# spring.datasource.password=eventure_pass
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# app.flyer.base-dir=C:/flyerFolder